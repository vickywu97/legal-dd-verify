# legal-dd-verify · 方案说明

> **任务**：买方（交易框架在 `pipeline/profile.py` 中定义，不属于任何实例答案）拟受让目标公司控股权；读取 52 项尽职调查核查清单与资料室（materials/），逐项核验资料、识别跨文件矛盾，输出核验反馈、补件问询、重点问题与机器可读清单。
> **交付物**：4 件标准化成果（核验总表 / 补件问询 / 重点问题摘要 / result_manifest.json）+ 可复现流水线 + 独立校验器。

---

## 一、整体架构

```
资料室(materials/)  ─┐
                    ├─► extract.py  ──► analyze.py ──► render.py ──► 4 件交付物
52项清单(checklist/)─┘     抽取层          规则+矛盾       渲染层
                                                    │
                                            verify.py（独立校验，不依赖分析引擎）
```

四层职责：

| 层 | 文件 | 职责 |
| --- | --- | --- |
| 抽取 | `extract.py` | 解析 PDF/XLSX/DOCX（及离线 `.txt` 视图），按 `XX.Y` 文档前缀建 `MaterialStore`，输出全文与结构化表。 |
| 分析 | `analyze.py` | 读取清单 + 抽取事实，产出 52 项核验（状态/事实/证据/缺口/风险/处置）、跨文件矛盾、补件问询（去重约 20 条）、12 项重点问题。 |
| 渲染 | `render.py` | 生成 4 件交付物，所有内容由抽取事实插值，无实例级硬编码答案。 |
| 校验 | `verify.py` | 独立一致性核查，作为质量门。 |

---

## 二、为什么可信（设计护栏）

| 风险 | 本方案的应对 |
| --- | --- |
| 写死答案 | 目标公司名、关联方、境外区域、年份、关键人、股权比例**全部运行期抽取**；唯一常量是 `profile.py` 的交易框架（买方 / 受让比例 / 基准日），属方案设计参数。 |
| 离线环境装包失败 | 依赖 vendored 于 `vendor/`（pypdf / openpyxl / et_xmlfile）；`.docx` 由自研纯标准库 `_docx.py` 替代 `python-docx`，规避 `lxml` C 扩展；运行期无需 `pip`、无需联网。 |
| 引用编造 | 每条证据精确到源文件名；独立校验器反向解析，引用必须落到真实源文件，否则判 FAIL。 |
| 核心风险漏评 | 股权/出资、核心知识产权、数据跨境、控制权变更四类**强制覆盖**，由校验器核对。 |
| 矛盾被淹没 | 跨文件矛盾结构化记录并挂接到相关清单项；矛盾"震中"项标记"资料冲突"，其余关联项仅做交叉引用，避免计数虚高。 |
| 预置成品 | 交付物由脚本运行期生成；仓库不捆绑预生成交付物（绑定演示结果仅作示例，可随时重跑覆盖）。 |

---

## 三、交付物一览

见 `README.md` 第三节。绑定原创演示数据室运行后，独立校验器结论为 **PASS**（52 项唯一、4 类核心风险覆盖、识别 3 处跨文件矛盾、0 处引用无法解析）。

---

## 四、跨文件矛盾检测（演示数据室示例）

| 编号 | 矛盾 | 关联清单项 |
| --- | --- | --- |
| CT-01 | 股权结构多源比例不一致（员工持股平台：台账 8% vs 章程/组织架构 10%） | CAP-01 / CORP-03 / CAP-04 / CAP-05 |
| CT-02 | 数据跨境表述与云架构实际部署不一致（隐私政策称无生产数据出境，云账单显示新加坡节点含个人信息） | DATA-03 / DATA-05 |
| CT-03 | 重大索赔/争议未纳入争议与或有负债清单（供应商丙逾期付款律师函未列入 07.1） | DISP-01 / DISP-02 / CON-08 |

矛盾通过比对文件**实质内容**（比例、金额、表述、部署区域）识别，而非仅依据文件名。

---

## 五、诚实边界

- 本方案是**方法论的可运行示例**，绑定一份原创合成演示数据室；事实抽取层针对该数据室文档结构编写，用于不同文档结构的真实项目需相应扩展。
- 输出为律师的支持性分析，**非**正式法律意见，不替代对登记机关、主管机关及相对方的独立核验。
- 演示数据室全部为虚构合成信息，由本仓库作者原创撰写，与任何真实主体、项目或第三方题库无关。
